<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrueMood" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bgColor</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Primary brand color. -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- for the bar that is above Toollbar -->
        <item name="android:statusBarColor">#ff3131</item>
    </style>

    <style name="Theme.TrueMood" parent="Base.Theme.TrueMood" />

    <!--    The Splash Screen Theme -->
    <style name="TheSplashScreen_Style" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowDisablePreview">true</item>
        <item name="android:statusBarColor">@color/splashBG</item>
    </style>

</resources>